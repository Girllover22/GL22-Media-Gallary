# Local Privacy Lock

- Network connections are denied by the renderer Content Security Policy.
- No cloud services, accounts, telemetry, analytics, uploads, remote metadata, or listening server.
- Media paths and the index remain in `data/` inside the application folder.
- The scanner only reads directory entries and file metadata.
- Original media is never changed automatically.
